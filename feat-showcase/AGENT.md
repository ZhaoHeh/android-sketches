# AGENT.md

## 模块角色

`feat-showcase` 是模板型模块，重点是演示接入方式，不是演示复杂功能。

## 修改建议

- 保持结构简单，方便作为复制模板
- 如果要扩展示例内容，优先展示“模块接入方式”，而不是堆业务逻辑
- 新模块可以参考本模块的目录结构、manifest 写法和 Activity 最小实现

## 不建议的方向

- 不要把它发展成第二个功能复杂模块
- 不要让它依赖额外重量级库，避免失去“最小模板”的价值

