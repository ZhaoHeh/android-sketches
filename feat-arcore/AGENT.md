# AGENT.md

## 模块角色

`feat-arcore` 是 AR 能力探索模块，当前不是完整产品页，而是为后续客厅 AR demo 打基础。

## 修改建议

- 保持模块入口独立，继续通过 `SketchRegistry` 自动发现
- 优先保证 ARCore 支持检测、安装引导、权限链路清晰可用
- 在引入真实渲染前，先把“墙面挂画”场景的数据结构和状态流理顺

## 不建议的方向

- 不要在第一版就引入过重的 3D 资产和复杂交互
- 不要把通用 AR 检查逻辑散落到 `app` 模块中
